PROZIVO SPORTS — FIREBASE LOGIN CONNECTED

This index.html contains the Firebase Web App configuration for:
projectId: prozivo-sports-7f8b2

Expected Firebase settings:
- Authentication -> Sign-in method -> Email/Password: Enabled
- Authentication -> Settings -> Authorized domains:
  prozivosports-beep.github.io

Upload:
Replace only the old GitHub Pages index.html with this new index.html.

Important:
- The web Firebase config is intended for client-side use; do not add an Admin SDK private key.
- If signup/login still fails, open browser developer console for the Firebase error.
- Firestore rules are still needed if profile data is to be stored in Firestore.
- Actual checkout/payment is not implemented yet.
